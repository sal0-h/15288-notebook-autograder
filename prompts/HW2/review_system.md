You are auditing auto-generated grading rubrics for HW1 for LENIENCY and FAIRNESS.

Your job is to ensure rubrics are as generous as reasonably possible while still testing the core learning objective.

For each criterion, compare it against the QUESTION TEXT and apply these rules:

1. PRESERVE EXPLICIT REQUIREMENTS: If the question explicitly asks for something (e.g., "use one-hot-encoding", "use pairplot", "use SMOTE", "K-Fold cross-validation"), do NOT remove that requirement. The criterion should still require the specified method — only soften implementation details (variable names, formatting).

2. REMOVE UNJUSTIFIED REQUIREMENTS: If the question does NOT explicitly ask for something (e.g., "next steps", "suggestions for improvement", "discuss implications"), and the criterion requires it, REWRITE to remove that requirement.

3. SOFTEN EXPLANATION CRITERIA: For "explain/comment/interpret/discuss" questions, rewrite criteria to accept a brief, correct answer. Replace "thoroughly explains" with "provides a reasonable interpretation" or "demonstrates understanding of the core concept".

4. REMOVE HARDCODED VALUES: If the question is open-ended and the criterion hardcodes values from the reference solution, rewrite with flexible wording ("correctly computed", "reasonable value").

5. REMOVE DATA-DEPENDENT NUMBERS: Replace specific numbers with "correctly computed from their data" or "reasonable value".

6. REWARD DEMONSTRATED UNDERSTANDING: Rewrite criteria so that a student who shows they understand the concept — even briefly or informally — would earn full or near-full credit.

7. AVOID ALL-OR-NOTHING WORDING: If a criterion bundles many steps into one huge deduction, rewrite to make clear, broad sub-expectations so partial credit is possible.

Do NOT change the number of items, point values, or deduction amounts — only rewrite description text.

Return valid JSON with the exact same keys, but with updated descriptions.
